<div class="container">
<%# スレッドの内容を表示 %>
    <div class="thread-box", style="margin: 10px 0px 10px 0px">
        <div class="panel panel-default">
            <div class="panel-heading">
                <h4><%= @board.title %></h4>
            </div>
            <div class="panel-body">
                <p class="panel-text"><%= @board.body %></p>
            </div>
        </div>
    </div>
<%# スレッドについたコメントを全件表示 %>
    <div class="all-comment-box">
        <div class="panel panel-default">
            <div class="panel-head">
                <h3>コメント一覧</h3>
            </div>
            <div class="panel-body">
                <% @comments.each do |comment| %>
                    <div class="comment-name">
                        <%= comment.name %>さん
                    </div>
                    <div class="comment-body">
                        <%= comment.comment %>
                    </div>
                    <div class="comment-date">
                        <%= comment.created_at.to_s(:datetime_jp) %>
                    </div>
                    <div class="line"></div>
                <% end %>
            </div>
        </div>
    </div>
<%# コメント欄を作成 %>
    <div class="imput-comment-box">
        <%= form_for(@comment) do |f| %>
            <%= f.hidden_field :board_id %>
        <div class="form-group">
            <%= f.label :name, '名前' %>
            <%= f.text_field :name, class:'form-control' %>
        </div>
        <div class="form-group">
            <%= f.label :comment, 'コメント' %>
            <%= f.text_area :comment, class:'form-control', size:"50x10" %>
        </div>
        <%= f.submit '送信', class:'btn btn-success' %>
        <% end %>
    </div>
<%# リンクを作成 %>
    <div class="top_button">
        <%= link_to 'トップページへ戻る', root_path, class: 'btn btn-default', data: {"turbolinks" => false} %>
    </div>
</div>